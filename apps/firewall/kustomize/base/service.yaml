# CHAPTER3_REFERENCE.md §3.0: Firewall service port 8080
apiVersion: v1
kind: Service
metadata:
  name: firewall
spec:
  selector:
    app: firewall
  ports:
    - port: 8080
      targetPort: 8080
      name: http
